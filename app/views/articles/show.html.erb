<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @article.body %>
</p>

<%= render "/articles/#{@type}/metadata" %>

<%= link_to 'Edit', edit_article_path(@article.becomes(Article)) %> |
<%= link_to 'Back', articles_path %>
